{
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "python main.py",
  "env": {
    "PORT": {
      "required": true
    },
    "TELEGRAM_BOT_TOKEN": {
      "required": false
    },
    "TELEGRAM_ADMIN_ID": {
      "required": false
    }
  }
}